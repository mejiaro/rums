<div class="container">
  <div class="row">
    <div class="col-9">
      <div class="row">
        <div class="col-9">
          <h1>Calendario</h1>
        </div>
        <div class="col-3">
          <strong>Ubicación: </strong><%= select_tag('room_id', options_for_select(Room.all.collect{ |u| [u.name, u.id] }), include_blank: true, onchange: "location.href='/?room_id='+$(this).val();") %>
        </div>
      </div>
    </div>
    <div class="col-3">
    </div>
  </div>
  <div class="row">
    <div class="col-9">
      <%= month_calendar events: @reservations do |date, reservations| %>
        <%= date %>
        <% reservations.each do |reservation| %>
          <div>
            <span class="badge badge-info">Reservado</span>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="col-3">
      <div class="row">
        <div class="col align-self-center">
          Nueva reserva
          <%= form_with scope: :reservation, url: reservations_path do |form| %>
            <%= form.select('room_id', options_for_select(Room.all.collect{ |u| [u.name, u.id] })) %>
            <%= form.date_field :start_time, required: true %>
            <%= form.submit %>
          <% end %>
        </div>
        <div class="col align-self-center">
          
        </div>
      </div>
    </div>
  </div>
</div>

